// require the media capture helper from rtc.io
var media = require('..');

// capture video and render it to the document body
media().once('capture', function(stream) {
  media({ stream: stream, muted: true }).render(document.body);
});
